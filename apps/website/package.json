{
  "name": "@camadb/website",
  "version": "0.0.0",
  "private": true,
  "description": "Public website for the open-source CamaDB database",
  "scripts": {
    "build": "next build && node scripts/copy-demo.js && yarn --cwd ../.. docs:build",
    "dev": "yarn --cwd ../.. docs:build --out apps/website/public/docs && node scripts/copy-demo.js --public && next dev",
    "start": "yarn dev",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@camadb/design": "0.1.0",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@camadb/core": "3.0.0-rc.0",
    "@camadb/memory": "0.2.0-rc.0",
    "@camadb/sync": "0.2.0-rc.0",
    "@camadb/knowledge-demo": "0.0.0"
  }
}
