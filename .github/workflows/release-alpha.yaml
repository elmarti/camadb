name: Alpha release

on:
  push:
    branches:
      - develop
    paths:
      - packages/**
      - .changeset/**
      - .github/workflows/release-alpha.yaml
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: release-alpha
  cancel-in-progress: false

jobs:
  release-alpha:
    name: Publish develop snapshots
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout develop
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Select alpha publication mode
        id: release-mode
        run: |
          if [ -f .changeset/pre.json ]; then
            echo "alpha=false" >> "$GITHUB_OUTPUT"
            echo "CamaDB is in committed RC mode; alpha publication is skipped."
          else
            echo "alpha=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Node.js
        if: steps.release-mode.outputs.alpha == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: yarn

      - name: Install Yarn Classic
        if: steps.release-mode.outputs.alpha == 'true'
        run: npm install --global yarn@1.22.22

      - name: Install dependencies
        if: steps.release-mode.outputs.alpha == 'true'
        run: yarn install --frozen-lockfile

      - name: Resolve affected workspaces
        if: steps.release-mode.outputs.alpha == 'true' && github.event_name == 'push'
        env:
          BASE_SHA: ${{ github.event.before }}
        run: echo "AFFECTED_WORKSPACES=$(node scripts/affected-workspaces.js "$BASE_SHA" "$GITHUB_SHA")" >> "$GITHUB_ENV"

      - name: Select all workspaces for a manual release
        if: steps.release-mode.outputs.alpha == 'true' && github.event_name == 'workflow_dispatch'
        run: echo "AFFECTED_WORKSPACES=$(node scripts/affected-workspaces.js --all)" >> "$GITHUB_ENV"

      - name: Validate affected workspaces
        if: steps.release-mode.outputs.alpha == 'true'
        run: yarn validate:affected

      - name: Create unique alpha snapshot versions
        if: steps.release-mode.outputs.alpha == 'true'
        run: yarn version:alpha

      - name: Rebuild versioned packages
        if: steps.release-mode.outputs.alpha == 'true'
        run: yarn build:affected

      - name: Publish to the alpha channel
        if: steps.release-mode.outputs.alpha == 'true'
        run: yarn release:alpha
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
