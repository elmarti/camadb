---
"@camadb/core": minor
---

Add bounded, read-only filesystem collection discovery, existence checks and declared column/index metadata through public Cama methods. Missing collections are never initialized by catalogue reads. Unsupported adapters fail explicitly.
