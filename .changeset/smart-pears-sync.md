---
'@camadb/sync': minor
---

Add the versioned synchronization protocol and local-to-local reference adapter with offline queueing, idempotent replay, explicit conflicts, and durable delete tombstones.
